export declare class ComputedValueService {
    constructor();
    concat(...values: string[]): string;
    add(...values: string[]): string;
    substract(...values: string[]): string;
    multiply(...values: string[]): string;
}
